{
  "name": "@image-gen/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "lsof -ti :7716 | xargs kill 2>/dev/null; secrets-run run --env-file=.env.local.tpl -- bun --watch src/index.ts",
    "dev:stack": "bun run dev",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@elysiajs/openapi": "1.4.16",
    "@image-gen/shared": "workspace:*",
    "elysia": "1.4.30",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "bun-types": "1.4.2",
    "typescript": "6.0.3"
  }
}
