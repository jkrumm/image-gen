// GENERATED FILE — do not edit by hand.
// Run `bun run generate:playbook` (from shared/) after editing shared/playbook/*.md.

export const PLAYBOOK: Record<string, string> = {
  article:
    '# Article / Blog Hero Images\n\nThe blog/article-specific case of `hero.md` — same negative-space discipline, narrowed to the editorial-post use case.\n\n- Use case first: _"hero banner for a blog post about X"_.\n- Reserve negative space deliberately: _"strong negative space above for title text; clear silhouette that reads at thumbnail size"_.\n- Prefer generating without embedded text and overlaying typography in post. If text must be in-image: quote it exactly and specify font feel, weight, color, placement (see `core.md`\'s "Text in images").\n- Template: `Create [type] for [use case]. Main subject: [...]. Composition: [framing, negative space]. Style and lighting: [...].` plus the target size.\n- Article art is typically exploratory and cheap: draft several at `quality: low` with a higher `n`, pick, then promote only the winner to `high` — see `settings.md`\'s cost anchors.\n',
  core: '# Core Principles\n\nCross-cutting prompting doctrine that applies regardless of the detected intent. `composePlaybookSystemPrompt` (`shared/src/playbook.ts`) always leads with this file, then `settings.md`, then `policy.md`, then the files for the detected/requested intent (`icons.md`, `hero.md`, `painterly.md`, `technical.md`, `article.md`, `diagram.md`, `figure-art.md`).\n\n## Core principles\n\n1. **Natural-language prose, not keyword tags.** GPT Image models read descriptive sentences. Midjourney-style tag spam ("8k, masterpiece, trending on artstation") is noise. Short labeled segments or line breaks are fine; comma-soup is not.\n2. **Ambiguity is the enemy.** Whatever the prompt leaves open, the model decides — confidently and often wrongly. Every property mentioned should have exactly one committed value. Never hedge ("such as", "for example", "or similar").\n3. **Constraints beat descriptions.** A 60-word prompt with tight constraints outperforms a 200-word adjective pile. Target ~60–120 words for a fully enhanced prompt (constraint block excluded).\n4. **Describe the job, not just the picture.** State the intended use early ("hero banner for a blog", "app icon", "UI mock", "infographic") — it sets the model\'s mode and polish level.\n5. **Medium/type first.** Open with the image type: photo, oil painting, watercolor, illustration, vector, 3D render.\n6. **Canonical ordering:** medium/type → scene/background → subject (lead with the concrete noun) → key details (composition/camera, lighting, palette/mood, style anchor) → exact text + typography → constraint block → the size/aspect intent.\n7. **Iterate like a director, not a gambler.** Base composition first, then lighting, then detail/clutter passes. Draft at `quality: low` (~$0.006), finalize at `high` (~$0.211, 35.8×) only when the composition is right.\n\n## Photorealism\n\n- Include the word _"photorealistic"_ or _"real photograph / taken on a real camera / iPhone photo"_.\n- Counter the AI look explicitly: _"real textures, natural skin, real fabric; no illustration, no CGI look; no beauty-retouching look; keep lighting neutral (avoid warm yellow tint)"_ plus imperfection signals (_subtle film grain, natural noise, visible pores_).\n\n## Negative constraints (there is no negative-prompt parameter)\n\n- Express exclusions inside the prompt, as a terminal constraint block: `Constraints: [what must not change], no watermark, no extra text, no logos, no clutter, no distortion.`\n- Prefer **affirmative phrasing** over negation where a positive exists — negation both mis-steers the model and (for sensitive nouns) trips the moderation classifier (see `policy.md`). "Plain background" beats "no busy background"; "wearing a full-coverage leotard" beats "not nude".\n- Defensive prompting against the model\'s default shortcuts: _"Do not stylize the face. Do not cartoonize."_ when realism matters.\n\n## Series consistency\n\n- Re-attach the reference image(s) on **every** call; reference inputs by index (_"Image 1: the product photo. Image 2: style reference — borrow its palette and lighting only."_).\n- Repeat a **preserve list** on every iteration: _"Preserve facial features, proportions, age, skin texture, hairstyle, and expression exactly."_ Describe only the delta; keep-list everything else (_"change only the background; keep everything else the same"_).\n- Reuse the series\' established vocabulary verbatim (medium, lighting, palette words) — vary only the declared difference.\n- Chained edits degrade (noise amplification after ~3–5 generations): branch from the original base image, not from the latest output. "Subtle refinements only, preserve original style, minimal changes" for touch-ups.\n\n## Text in images\n\n- gpt-image-2 renders Latin text at ~95–99% accuracy; older models are unreliable.\n- Quote literal text: `Headline: "SUMMER COLLECTION"`. Unquoted text is treated as a suggestion.\n- Specify typography: font feel, weight, color, placement. Spell tricky words letter-by-letter. List each language explicitly for multilingual text.\n- If spelling fails: less text, larger type, regenerate. Text-heavy → `quality: high` (worth the cost — see `settings.md`).\n\n## Camera and lighting vocabulary\n\n- Camera terms are **look-and-feel signals, not optical simulation**. Reliable: focal-length classes (35mm natural / 50mm neutral / 85mm portrait compression), framing (close-up, wide shot, flat lay, top-down, low angle, eye level), aperture _behavior_ ("shallow depth of field, f/1.8 feel" vs "everything in focus"), film stocks as style anchors ("shot on 35mm film", "Kodachrome").\n- Lighting: give **direction**, not just quality — _"soft window light from the left, shadows falling right"_; named patterns (three-point, rim light, Rembrandt, golden hour) are recognized.\n- Depth: name foreground / midground / background contents explicitly.\n\n## Failure modes quick reference\n\n| Failure                                           | Mitigation                                                                                           |\n| ------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |\n| Mangled hands/anatomy                             | "anatomically correct hands, natural hand position"; avoid extreme poses; hide hands compositionally |\n| AI look (plastic skin, warm tint, oversaturation) | Photorealism counters above; quality high for portraits                                              |\n| Text gibberish                                    | See "Text in images" above; gpt-image-2 only for text-heavy work                                     |\n| Wrong object counts                               | Keep counts ≤5; don\'t rely on numbering                                                              |\n| Iterative drift / overcooking                     | Branch from the base image; preserve lists; "subtle refinements only"                                |\n| Strict isometric drift                            | Treat as style cue, not projection; post-cleanup for precision (see `technical.md`)                  |\n| Waiting for N streaming partials                  | Upstream may send fewer than requested — never build UI that waits for a fixed count                 |\n',
  diagram:
    '# Diagrams and Infographics\n\nTitle → visual structure → color constraints → anti-clutter: _"clean boxes and arrows, readable sans-serif typography, one accent color, no extra words, no decorative clutter"_. Dense text warrants `quality: medium` while drafting the structure, and `high` only once the layout is settled — see `settings.md`\'s quality rules and `core.md`\'s "Text in images".\n',
  'figure-art':
    '# Fine-Art Figure Subjects (Craft Guidance)\n\nCraft guidance only for describing sensitive-but-allowed artistic subjects — figure studies, dance, fashion/latex editorial, fine-art rope work, implied/draped figures — in classifier-legible terms. Policy mechanics, hard boundaries, and the general trigger table live in `policy.md`; this file is the recipe for staying on the allowed side of that boundary.\n\nThe craft is describing the allowed reading explicitly:\n\n1. Name the artistic tradition, medium, and venue: _"fine-art figure study in the tradition of academic life drawing, charcoal on paper"_, _"gallery exhibition print"_. Non-photorealistic media pass where photorealism blocks.\n2. State clothing/coverage affirmatively and specifically.\n3. Describe pose by dynamics and form (_"extended line, arched spine, controlled balance"_), garments by material and cut, never by allure.\n4. Use the classical implied-figure toolkit: sheer draping, rim/cross light defining a silhouette, back views, shadow concealment, tight crops, _"painterly, timeless quality"_.\n5. Expect restraint-adjacent and exposure-adjacent combinations to hit the output classifier regardless of phrasing; probe once cheaply (`quality: low`) before committing to a series, and treat a persistent block as the boundary, not a puzzle.\n',
  hero: '# Hero / Banner / Cover Images\n\nThe general recipe for a hero, banner, or cover image — a blog post\'s hero image specifically is `article.md`; a painterly journal-cover series is `painterly.md` for the medium vocabulary, this file for the layout discipline.\n\n- Use case first: state the intended placement ("hero banner", "landing-page cover", "wallpaper") — it sets composition and polish level.\n- Reserve negative space deliberately for what will sit on top of the image: _"strong negative space above/beside for title text; clear silhouette that reads at thumbnail size"_.\n- Prefer generating without embedded text and overlaying typography in post-production — more flexible than baking text in. If text must be in-image, follow `core.md`\'s "Text in images" rules exactly (quoted text, spelled-out typography).\n- Template: `Create [type] for [use case]. Main subject: [...]. Composition: [framing, negative space]. Style and lighting: [...].` plus the target size.\n',
  icons:
    '# Icons, Logos, Flat Graphics\n\n- Trigger vocabulary: _flat design, vector-like shapes, strong silhouette, balanced negative space, minimal strokes, no gradients unless essential, plain background, single centered subject_.\n- Always include _"original, non-infringing"_. Describe brand personality before visual elements ("warm, simple, timeless").\n- Demand small-size legibility: _"reads clearly at small and large sizes — avoid fine detail or busy backgrounds"_.\n- Square 1024×1024; _"no text unless specified"_.\n- **Transparency is not currently available** — no model in the generate path can emit alpha. Render icons on a _plain solid white background_ (say so explicitly in the prompt, never "transparent" or "cutout"), and note in `assumptions` that the background may need removing manually. See `settings.md` → "Background treatment".\n',
  painterly:
    '# Painterly / Oil / Impressionist\n\nFor journal covers and fine-art work.\n\n- Name the movement or school explicitly: "Impressionist", "Hudson River School", "mid-19th century American landscape painting". Avoid living artists by name (see `policy.md`) — use movement/era/described aesthetic instead.\n- Specify physical-medium qualities: _visible brushstrokes, impasto texture, palette-knife energy, canvas weave, no smooth blending, oil-paint luminosity_.\n- Anchor palettes with named pigments: _sapphire blue, viridian green, carmine red, golden ochre_.\n- Covers: _"editorial magazine cover layout"_, negative space reserved for title text, wide atmospheric scene. 2560×1440 works (gpt-image-2 only); larger is experimental.\n- Series (e.g. a journal-cover run): reuse the established vocabulary verbatim (medium, lighting, palette words) across the series and vary only the declared subject — see `core.md`\'s "Series consistency".\n',
  policy:
    '# Content-Policy Boundaries (Compliance Craft)\n\nPurpose: predict refusals before spending a request, and phrase **policy-compliant** work so it isn\'t false-positive refused. Never an evasion tool — hard categories stay hard, and the enhancer warns instead of contorting.\n\nCraft guidance for the sensitive-but-allowed artistic subjects (figure studies, dance, fashion/latex editorial, fine-art rope work, implied/draped figures) lives separately in `figure-art.md` — this file stays mechanics-and-triggers only.\n\n## Mechanics\n\nModeration runs in two stages: an input classifier on the prompt (and on edit reference images) and an output classifier on the generated image. Input blocks are deterministic — the same prompt always fails; retrying is futile; rewrite instead. Output blocks are scene-level — rewording alone won\'t fix them; change composition/coverage/medium. The API error is `moderation_blocked` (`image_generation_user_error`; arrives 503-wrapped through our upstream as a **string-prefixed body** — extract the embedded JSON — never retried by the gateway) with `moderation_details: { moderation_stage: input|output, categories }` (categories coarse; `"other"` observed). The `moderation: low` parameter reduces false positives on artistic/classical subjects but moves no hard boundary; probe-verified 2026-07-17: **accepted on `/images/edits`** by gpt-image-2 and 1.5.\n\n## Calibration (probe-verified 2026-07-17)\n\nOur upstream is more permissive than public-API reports: a photoreal named-celebrity portrait generated fine, while a living-artist style blocked at input and a franchise character blocked at output. The trigger tables below describe _public-API_ behavior — treat pre-check verdicts as advisory warnings, not gates, except for the hard walls.\n\n## Hard boundaries — warn, don\'t rewrite\n\nExplicit sexual content; anything involving minors combined with realism (never combine minors + photorealism + close-up even innocuously); photorealistic identifiable real people (generation or edit — the classifier can\'t verify consent, including for the user\'s own photo); individual living artists\' styles by name; realistic gore; hate symbols.\n\n## False-positive triggers and rewrites\n\n| Trigger                                                                        | Rewrite strategy                                                                                   |\n| ------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------- |\n| Living artist name                                                             | Studio/movement/era + three style adjectives + medium ("Studio Ghibli style", "ukiyo-e woodblock") |\n| Celebrity / real person                                                        | Described archetype if likeness isn\'t the point; otherwise warn — hard boundary                    |\n| Franchise characters (stricter than DALL·E 3 was)                              | Generic visual traits, original character in the genre style                                       |\n| Arousal-coded adjectives ("sexy", "seductive", "revealing", "tight", "skimpy") | Craft vocabulary: elegant, sculptural, tailored, fitted, high-fashion editorial                    |\n| Negated sensitive nouns ("no nudity")                                          | Strip the negation; state attire affirmatively ("draped in floor-length ivory silk")               |\n| Weapon/violence nouns in realistic register                                    | Stylized/cinematic/historical-illustration framing; motion and lighting instead of gore            |\n| Reference photo with a real face or visible skin (edits)                       | Warn up front; suggest stylized source or generation instead                                       |\n| Non-English prompt with ambiguous terms                                        | Offer the English translation                                                                      |\n| Trivially innocent prompt blocked                                              | Possible upstream moderation incident — retry later, don\'t contort                                 |\n\n## Enhancer duties\n\nRun the pre-check on every brief; annotate the risk and the rewrite it applied (visibly, in `assumptions`); suggest `moderation: low` when the subject is artistic and the block class is known false-positive-prone; never auto-retry a moderation block.\n',
  settings:
    '# Settings Derivation\n\nThis studio\'s rules for turning a brief into concrete generation settings. The enhancer derives these; every derived value surfaces as an editable control, never hidden. Explicit user values are echoed verbatim and never overridden — see `overrides` in the `/enhance` v2 contract (`shared/src/plan.ts`).\n\n## Quality and n — draft-first by default\n\n**This is the most expensive decision you make. Get it right before anything else.**\n\n`high` costs **35.8×** what `low` costs ($0.211 vs $0.006 per image at 1024×1024). This studio is an iteration loop, not a one-shot renderer: the user almost always wants several cheap candidates to choose from, then promotes the winner. Promoting later is one click; money spent on a high-quality render of the wrong composition is gone.\n\n**Default, applied unless a rule below overrides it: `quality: low`, `n: 4`.**\n\nEscalate only on an explicit signal:\n\n| Signal                                                                                | quality  | n   |\n| ------------------------------------------------------------------------------------- | -------- | --- |\n| _(no explicit signal — the common case)_                                              | `low`    | 4   |\n| "draft / try / explore / variations / options / ideas"                                | `low`    | 4–6 |\n| "final / print / ship / publish / for the site" — an explicit finalization request    | `high`   | 1   |\n| Dense rendered text is central to the image (a poster\'s headline, a diagram\'s labels) | `medium` | 2   |\n| Iteration on an accepted prompt (a delta re-run) whose parent already ran `high`      | `high`   | 1   |\n| The policy pre-check predicts an **input** block (`predicted_stage: "input"`)         | `low`    | 1   |\n\nRules for combining them:\n\n- **`high` implies `n: 1`.** Never propose `high` with `n > 1` — that is a $0.85+ request off a single brief. If the brief wants both variety and finish, propose `low` with a high `n` and say in `assumptions` that the winner should be promoted.\n- **A subject being hard is not a finalization signal.** Photoreal portraits, intricate icons, and detailed illustrations still _draft_ at `low`. Difficulty is a reason to iterate more, not to pay 35.8× on the first attempt.\n- **Never spend `high` on a request you predict will fail.** If the policy pre-check raises a `hard`- or `rewrite`-severity input-stage warning, the request likely never reaches the renderer — derive `low`/`n: 1` and say why in `assumptions`.\n- Whatever you derive, name the two-step in `assumptions` when you chose `low` for something the user may consider finished work (e.g. "drafting 4 at low ≈ $0.024; promote the winner to high ≈ $0.211").\n\n## Model — one model, always\n\n**This studio generates on `gpt-image-2` and nothing else.** Never propose `gpt-image-1.5` or `gpt-image-1-mini`; they are retired from the generate path. (They still appear in the library on old generations — that is the read path, and it is none of your concern when deriving settings.)\n\nBecause there is only one model, there is no routing decision left to make. What follows is about size and the remaining parameters.\n\n## Size — arbitrary `WxH` is always available\n\ngpt-image-2 accepts **arbitrary custom sizes on both generate and edit**. There are no presets-only cases anymore; never constrain a size because "the model only takes presets". The envelope:\n\n- width and height are **multiples of 16**\n- aspect ratio **≤ 3:1** in either direction\n- total area **655,360–8,294,400 px**\n- longest edge **< 3840**\n\nDerive the size the brief actually wants and let `shared/src/rules.ts` snap it if it lands off-grid.\n\n## The rest\n\nThese derive from the brief\'s _shape_, independent of the quality decision above:\n\n| Signal in the brief                                     | Derivation                                                                                                                                                                                             |\n| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| Icon / logo / avatar / sticker                          | square 1024; plain solid white background — see "Background treatment" below                                                                                                                           |\n| Cover / banner / hero / wallpaper                       | wide custom size; 2560×1440 for journal covers                                                                                                                                                         |\n| Poster / portrait subject / phone                       | tall — 1024×1536, or a custom tall `WxH`                                                                                                                                                               |\n| Transparency ("transparent", "cutout", "no background") | **not available** — derive `background: opaque` with a plain solid white background, and say so in `assumptions`. See "Background treatment"                                                           |\n| Edit with identity/product fidelity concerns            | nothing to derive — **never send `input_fidelity`**. gpt-image-2 rejects the parameter outright (hard 400) and is locked to high fidelity internally, so the behaviour you want is already the default |\n\nThe capability matrix is enforced by `shared/src/rules.ts`, not here — this table only describes what to _derive_; `rules.ts` disposes.\n\n## Background treatment: transparency is currently unavailable\n\n**The studio has no model that can emit transparency.** gpt-image-2 has no alpha channel and hard-400s on `background: "transparent"` (probe-verified), and it is the only model in the generate path. This is a deliberate, temporary capability regression, not an oversight.\n\nSo: **never derive `background: "transparent"`.** For any brief that wants a cutout, a sticker, a logo, or an icon "with no background":\n\n- derive `background: "opaque"` and put **"on a plain solid white background"** into the prompt text as an explicit rendering instruction;\n- state plainly in `assumptions` that **transparency is not currently available and the asset may need its background removed manually**. Never let the user believe they received a transparent PNG when they did not.\n\nThe restoration path, when it is built, is local matting on that plain-white render (Apple Vision `VNGenerateForegroundInstanceMaskRequest` — on-device, permissive, zero dependencies; see `docs/research/transparency-and-vector.md`). Generating on white is therefore not just a fallback, it is the correct input for that pipeline.\n\n### Prompt text must match the `background` parameter\n\n**The prompt text must never assert a background treatment that contradicts the `background` parameter this request sends.** Probe-verified failure: send `background: "opaque"` while the prompt says "isolated on a transparent background", and the model does not ignore the mismatch — it **paints a fake transparency checkerboard directly into the image pixels**. The output has no alpha channel (opaque RGB, colortype 2) and the checkerboard is baked-in garbage, not real transparency. This is a real, previously-shipped bug caused by exactly this contradiction. Since `background: transparent` is now never derivable, the rule reduces to a flat prohibition:\n\n- **Never emit the words "transparent background" (or "cutout", "no background", or any checkerboard/transparency-pattern description) into prompt text.** There is no longer any request shape in which those words are correct. This is the exact failure above, by name, on record so it doesn\'t get "simplified away" in a future edit.\n- **Always specify the background explicitly** — default to "on a plain solid white background", phrased as a deliberate rendering instruction, never left implicit.\n- **Never suggest chroma-key / green-screen generation as a transparency workaround.** It is a separate, documented failure — colour leakage onto the subject, non-uniform "solid" colours, 1px halos (`docs/research/transparency-and-vector.md`) — not a fallback for cases where alpha isn\'t available.\n\n## Cost anchors\n\nCost anchors (probe-measured, gpt-image-2 @ 1024×1024): low ≈ $0.006, high ≈ $0.211 per image; streaming preview costs a flat ~+$0.002 — default it on. Implemented as `estimateCost()` in `shared/src/cost.ts`, which scales these anchors linearly with pixel count relative to 1024×1024. Anchors are per-model; since gpt-image-2 is the only model you can derive, these are the only numbers you need.\n',
  technical:
    '# Sharp / Technical (Product, UI, Isometric)\n\nProduct renders, UI mockups, and isometric/exploded technical art. Diagrams and infographics are their own recipe — see `diagram.md`.\n\n- Product: _"Modern minimalist product photo of [PRODUCT] on [SURFACE], soft window light from the left, neutral background, shallow depth of field, professional product photography"_ + material cues (_matte black ceramic, condensation droplets, sharp catchlights, subtle drop shadow_).\n- UI mockups: _"Flat design illustration of a [domain] dashboard mockup with [components]... modern SaaS branding"_; name the platform idiom (_"realistic iOS-style status bar"_, _"Material Design 3"_); for flows, describe each screen individually (_"four phone screens arranged horizontally with arrows connecting each step"_).\n- Isometric: strict projection is unreliable and drifts across iterations. Use _"professional 3D game art style, consistent 45-degree angle"_ as the cue; exploded views work (_"exploded assembly view"_, _"transparent cutaway"_). Precise engineering drawings need post-cleanup — say so rather than promising.\n',
  texture:
    '# Textures and patterns\n\nRecipe for seamless textures, repeating patterns, and material swatches. Thinner doctrine than the other intents — extend it as real usage teaches more.\n\n- Say **"seamless repeating pattern, tileable edges"** explicitly when tiling matters; nothing about tiling is implied by "texture" alone. Verify tiling by eye — the model approximates it, and edges may still need post-cleanup.\n- Lead with the **material, not the scene**: _"brushed steel surface"_, _"hand-thrown ceramic glaze"_, _"woven linen weave"_. Concrete material and finish words carry the result: matte, glossy, grainy, brushed, woven, cracked, weathered, embossed.\n- **Flat, even, diffuse lighting; no focal subject; uniform density edge to edge.** A texture with a highlight or a hero element tiles badly and reads as a photo, not a surface.\n- **Top-down / straight-on view** unless a specific relief angle is wanted; add _"consistent scale across the surface"_ for pattern work.\n- Anchor the palette with named colors or supplied hexes; one or two colors for pattern work, or the pattern drifts.\n- Square sizes tile best; draft at `quality: low` — texture quality differences between tiers are smaller than for subjects, so promote only when detail genuinely matters.\n- Constraint block: _"no text, no watermark, no focal object, no vignette, uniform lighting"_.\n',
}
